# empty -- an e2e fixture for lib.sh's publish_fixture.
#
# Explicit "wg: {}": zero WireGuard interfaces, a legitimate bundle
# value, not an error (unlike node add's own fully-commented-out
# template, which is deliberately invalid YAML content -- see
# cmd/stunmesh-provd/node_cmd.go's wgYAMLTemplate doc).
{}
